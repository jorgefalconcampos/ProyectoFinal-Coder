<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
     <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-wEmeIV1mKuiNpC+IOBjI7aAzPcEZeedi5yW5f2yOq55WWLwNGmvvx4Um1vskeMj0" crossorigin="anonymous">
      <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css">
    <!-- Bootstrap Font Icon CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.5.0/font/bootstrap-icons.css">
    <title>Coderhouse</title>
</head>
<body>
    <nav class="navbar navbar-expand-md navbar-dark bg-dark mb-2">
        <div class="container-fluid">
            <a class="navbar-brand abs" href="#">Navbar</a>
            <button class="navbar-toggler ms-auto" type="button" data-bs-toggle="collapse" data-bs-target="#collapseNavbar">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-collapse collapse" id="collapseNavbar">

                 {{!-- <a href="/api/producto">Productos</a>
        <a href="/api/usuario">Usuarios</a>
        <a href="/session">Login</a>
        <a href="/register">Registrar</a>
        <a href="/logout">Logout</a>
 --}}

                <ul class="navbar-nav">
                    <li class="nav-item active">
                        <a class="nav-link" href="/api/products">Productos</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/register" >Registrar</a>
                    </li>
                </ul>
                <ul class="navbar-nav ms-auto">
                    {{#if username}}
                        <li class="mx-1 nav-item my-auto">
                            <span class="badge bg-primary py-2">Bienvenido {{username}}</span>
                        </li>
                        <li class="mx-1 nav-item my-auto">
                            <span class="pl-3 badge bg-secondary py-2">Rol: {{role}} </span>
                        </li>
                        <li class="nav-item">
                            <a class="nav-link" href="/logout">Logout</a>
                        </li>
                    {{else}}
                        <li class="nav-item">
                            <a class="nav-link" href="/">Login</a>
                        </li>
                    {{/if}}
                </ul>
            </div>
        </div>
    </nav>
    <div class="container my-4">
        {{{ body }}}
    </div>
</body>
 <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0/dist/js/bootstrap.bundle.min.js" integrity="sha384-p34f1UUtsS3wqzfto5wAAmdvj+osOnFyQFpp4Ua3gs/ZVWx6oOypYoCJhGGScy+8" crossorigin="anonymous"></script>
</html>